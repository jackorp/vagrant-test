summary: Custom synced folder path with default options.
test: |
    vagrant up && \
    (vagrant ssh -c "cat /lizmap/main.fmf" | grep "Synced folder") && \
    vagrant destroy -f
duration: 30m
contact:
  - jprokop@redhat.com
  - pvalena@redhat.com

tag:
  - nfs
