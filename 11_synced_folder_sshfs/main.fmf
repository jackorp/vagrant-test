summary: Shared synchronized folder via sshfs
test: |
    vagrant up && \
    (vagrant ssh -c 'ls /vagrant 2>/dev/null' | grep 'test.cdfcdeg.test') && \
    vagrant destroy -f
duration: 30m
contact:
  - jprokop@redhat.com
  - pvalena@redhat.com

tag:
  - sshfs
  - sync_folder
