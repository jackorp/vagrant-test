summary: Shared synchronized folder via sshfs
test: |
    ./test_vagrant.sh "vagrant ssh -c 'ls /vagrant 2>/dev/null' | grep 'test.cdfcdeg.test'"
duration: 30m
contact:
  - jprokop@redhat.com
  - pvalena@redhat.com

tag:
  - sshfs
  - sync_folder
