summary: Vagrant VM configured with additional disk.
test: |
    vagrant up && \
    (vagrant ssh -c 'lsblk' | grep '20G') && \
    vagrant destroy -f
duration: 30m
contact:
  - jprokop@redhat.com
  - pvalena@redhat.com
