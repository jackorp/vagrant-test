summary: Set a custom hostname in a VM.
test: |
    vagrant up && \
    (vagrant ssh -c "hostname" | grep "this.is.my.vagrant.box") && \
    vagrant destroy -f
duration: 30m
contact:
  - jprokop@redhat.com
  - pvalena@redhat.com
tag: base
